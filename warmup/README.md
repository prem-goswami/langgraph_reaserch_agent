# Warm-up graph

The 3-node conditional graph built first to establish LangGraph mechanics
before the research agent: router → (conditional) → search → generation.
No parallelism, no reducers, no correction loop — deliberately minimal so
each mechanic was visible in isolation.

Not part of the deployed service. Kept as build history; see
`docs/Warmup_Graph_Build_Record.docx`.
